ordenação inversa (para numeros ou textos)
variável.sort(reverse=True)
criar uma nova lista de uma lista existente
*função copy(); ou
*função list().
ex: novaLista = listaExistente.copy()
ex: novaLista = list(listaExistente)
função para verificar quantas vezes um objeto ocorre dentro de uma lista
ex: variável.count(objeto)
print(quantidade) # imprime 3
verificar a 1 posição de um objeto de uma lista
ex:

intervalos - função range ()
Um intervalo é definido por:
*um valor inicial
*um valor final; e
*um valor de passo
A função range gera sequencia e define intervalo de valores
sintaxe
range(início, fim, passo)
